# Third-party ground-truth disassembly (clone separately — see README)
pokered-master/

# Design-conversation transcripts (local only)
cctranscript/

# Project memory store / local tooling
.mnemo/
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# World-model artifacts (regenerable: gen_data.py / train_torch.py / dagger.py)
poc/model_torch.pt
poc/model.npz
poc/model.json
poc/transitions*.jsonl
poc/_overfit.jsonl
poc/map_grid.txt

# scratch captures & archives
poc/shot_*.png
poc.zip
poc/poc.zip
